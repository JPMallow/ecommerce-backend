<div class="card">
  <div class="card-header">
    Category Details
  </div>
  <div class="card-body">
    <h5 class="card-title"><%= @category.name %></h5>
    <%= link_to 'Edit', edit_category_path(@category), class: 'btn btn-primary' %> |
    <%= link_to 'Back to List', categories_path, class: 'btn btn-secondary' %>
  </div>
</div>