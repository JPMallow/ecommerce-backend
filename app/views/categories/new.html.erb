<div class="card">
  <div class="card-header">
    <%= @category.new_record? ? 'New Category' : 'Edit Category' %>
  </div>
  <div class="card-body">
    <%= render 'form', category: @category %>
  </div>
</div>