<div class="card">
  <div class="card-header">
    Subcategory Details
  </div>
  <div class="card-body">
    <h5 class="card-title">Name: <%= @sub_category.name %></h5>
    <div>Category: <%= @sub_category.category.name %></div>
    <br>
    <%= link_to 'Edit', edit_sub_category_path(@sub_category), class: 'btn btn-primary' %> |
    <%= link_to 'Back to List', sub_categories_path, class: 'btn btn-secondary' %>
  </div>
</div>